# --- Core AI/ML ---
openai>=1.3.0,<2.0.0
faiss-cpu~=1.7.4
numpy~=1.23.5

# --- Web API Framework ---
# FastAPI và Pydantic V1 cho Python 3.9
fastapi~=0.99.1
pydantic~=1.10.12
# Uvicorn: ASGI server
uvicorn[standard]~=0.23.2

# --- Utilities ---
python-dotenv~=1.0.0
# httpx: Client HTTP không đồng bộ, thường được OpenAI/FastAPI sử dụng
httpx>=0.24.0,<0.28.0
# Requests: Có thể vẫn cần cho các tương tác HTTP đồng bộ khác
requests~=2.31.0

# --- Các thư viện khác có thể cần thiết gián tiếp ---
# Jinja2: FastAPI có thể sử dụng cho templates (không dùng ở đây nhưng an toàn để giữ)
Jinja2~=3.1.2
# typing-extensions: Cần cho type hints
typing-extensions~=4.5.0
